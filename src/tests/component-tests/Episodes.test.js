import { render, screen } from '@testing-library/react';
import Episodes from '../../components/Episodes';

describe("Episodes", () => {
    it('should', () => {
        render(
            <Episodes />
        );
    })
})